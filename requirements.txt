# FIX: dotenv → python-dotenv (dotenv==0.9.9 เป็นคนละ package)
python-dotenv==1.1.0
Flask==3.1.3
flask-cors==6.0.2
langchain-core==0.3.58
langchain-google-genai==2.1.4
novita-client==0.7.1
# FIX: openai==2.30.0 ไม่มี version นี้ — SDK จริงอยู่ที่ 1.x
openai==1.82.1
supabase==2.15.2
waitress==3.0.2
# FIX: pin gunicorn version เพื่อให้ deployment reproducible
gunicorn==23.0.0